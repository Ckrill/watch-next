const settings = {
  endpoint: "https://api.themoviedb.org/3/",
  key: "api_key=83b296315507b7ea0ccdcc536a5ab745"
};

// Example url
// const url = endpoint + mode + "&" + key + "&page=" + page + genre + query;

export default settings;
